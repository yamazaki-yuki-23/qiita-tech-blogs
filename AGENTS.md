# AGENTS.md

## 基本方針

- チャットの応答は日本語で行う。
- このリポジトリの主な成果物は、`public/` に置く Qiita 向けの技術記事である。
- ユーザーが記事の「レビュー」「添削」「改善」を依頼した場合は、指摘だけで終わらせず、指定された記事へ修正を反映する。
- 著者の意図や文体は残す。ただし、事実誤認、誤解を招く表現、初心者が理解しにくい構成は修正する。

## 記事レビューの品質基準

### 1. 公式情報と照合する

- 技術仕様、API、製品の対応状況、リリース内容は、必ず現在の公式情報で確認する。
- 情報源は、原則として次の優先順位で選ぶ。
  1. 公式仕様、標準規格、公式リファレンス
  2. 公式リリースノート、公式ブログ
  3. 公式 SDK のコード、型定義、移行ガイド
- 第三者の記事や検索結果の要約だけを根拠にしない。
- 記事内の重要な主張を一つずつ確認する。特に次を重点的に見る。
  - 用語と登場人物の定義
  - バージョン番号と公開日
  - 追加、変更、非推奨、削除の区別
  - API 名、フィールド名、ヘッダー名、エラーコード
  - 必須・推奨・任意の区別
  - 対応するリクエストや機能の範囲
  - 新旧バージョンの互換性
  - 製品や SDK の対応状況
- 公式情報が述べていない内容を、確定事項として書かない。推測を残す必要がある場合は、推測だと明記する。
- 「不可能」「必ず速くなる」「作業はない」などの強い断定は、公式情報が直接裏付けている場合だけ使う。
- 直接の代替機能がないものを、似た機能で一対一に置き換えたように説明しない。
- 機能全体の非推奨と、個別の API やフィールドの削除を分けて確認する。
- 仕様で推奨されている動作を、必須の動作として断定しない。

### 2. 初心者が前から順に理解できる構成にする

- 読者は、その技術を知らない状態から読み始めると想定する。
- 必要な用語は最初に短く定義し、定義前の専門用語を重ねない。
- 「何か」「なぜ必要か」「何が変わったか」「自分に何が関係するか」の順で説明する。
- ホスト、クライアント、サーバーなど、似た役割を省略して一つにまとめない。簡略化する場合は、厳密な定義を先に示す。
- 抽象的な説明の直後に、具体例、表、または小さな図を置く。
- 処理の順序や通信の流れは、文章だけで分かりにくければ Mermaid のシーケンス図を使う。
- 比較や対応関係は表にする。ただし、本文で十分に説明できる内容まで表にしない。
- 読者の立場が複数ある場合は、「使う側」「実装する側」などに分ける。
- セッション、リクエスト、レスポンスなどの一般的な技術用語も、対象読者が知らないと想定される場合は初出で短く説明する。
- 「状態」「仕組み」「情報」などの抽象的な言葉だけで済ませず、実際に何を保存・送信・処理するのかを書く。

### 3. 難しい日本語と翻訳調を避ける

- 一文には原則として一つの内容だけを書く。長い文は分ける。
- 段落は原則 4 文以内にする。
- 一般的な言葉で説明できる場合は、難しい熟語やカタカナ語を使わない。
- 専門用語を使う場合は、最初に日本語で意味を説明する。
- 原文の語順をそのまま移した、不自然な翻訳調にしない。前後の文が自然につながる日本語へ書き直す。
- 主語と対象を省略しすぎない。「誰が」「何を」行うのかが分かる文にする。
- 「これ」「それ」「この仕組み」が何を指すか曖昧な場合は、具体的な名前に置き換える。
- 「サーバー発」「突き返す」などの省略語や話し言葉は避け、誰が何をするのかをそのまま書く。
- MUST / SHOULD / MAY を扱う記事では、「必須 / 推奨 / 任意」を明確に訳し分ける。
- コード、API、フィールド、ファイル名はバッククォートで囲む。
- 同じ用語の表記を統一する。例として、「ヘッダ」と「ヘッダー」を混在させない。

### 4. 簡潔にする

- 結論と記事を読む目的を冒頭に置く。
- 本題に入る前の個人的な経緯、一般論、あいさつは、理解に必要な場合だけ残す。
- 同じ結論を言い換えて繰り返さない。
- 仕様の列挙だけで終わらせず、読者の理解に必要な理由と影響だけを説明する。
- 例は一つの概念につき一つを基本とする。
- 「言うまでもなく」「簡単に」「当然」「〜にすぎません」など、読者の理解度を決めつける表現を使わない。
- 比喩は理解を助ける場合だけ使い、比喩だけで仕様を説明しない。
- まとめでは本文全体を再説明せず、中心となる変更と読者の次の行動に絞る。

### 5. コードと図を検証する

- JSON、HTTP、コマンド、コード例は、可能な限り公式仕様の例または型定義と照合する。
- 断片だけでは配置場所が分からない例は、最小限の完全な形に直す。
- 省略した必須フィールドがある場合は、省略していることを明記する。
- 説明用に作ったフィールド名や値は、仕様で定義されたものと区別し、「説明用の例」であることを明記する。
- 任意のフィールドを例や図に載せる場合は、常に必要なフィールドに見えないよう、使う条件を添える。
- 初回リクエストと再試行などを並べる場合は、IDや変更箇所が読者に分かるようにする。
- Mermaid 図の内容が本文と矛盾していないか確認する。
- 図がQiitaでレンダリングできることを確認する。

### 6. 一文ずつ曖昧さを取り除く

- 本文だけでなく、見出し、表のセル、図のラベル、コードコメントも先頭から一つずつ確認する。
- 各文について、「誰が」「何を」「どうするのか」「指示語が何を指すのか」が一度で分かるか確認する。
- 用語が原文でどのように使われているかを確認する。訳語だけでは意味が伝わらない場合は、その技術における意味を短く補う。
- 一つの問題を見つけたら、その表現だけでなく、同じ種類の曖昧さや誤解が記事全体にないか検索する。
- 指摘された一文だけを直して完了にせず、見出し、要約、本文、表、図の説明が同じ内容になっているか確認する。

## 必須のレビューフロー

記事のレビュー・添削では、次の順番を守る。

1. 対象の記事を先頭から末尾まで読む。
2. 記事の対象読者と目的を一文で定義する。
3. 重要な主張、数値、日付、API 名、必須条件を洗い出す。
4. 公式情報を検索し、主張ごとに照合する。
5. 次の優先順位で本文を修正する。
   1. 事実誤認と仕様上の誤り
   2. 読者に誤解を与える断定や省略
   3. 構成と前後関係
   4. 難しい表現と翻訳調
   5. 重複と冗長な説明
   6. 表記の揺れ
6. 修正後の記事を、初めて読む読者の視点で一文ずつ通読する。
7. 問題を見つけるたびに、同じ表現と同じ種類の問題を記事全体から検索して修正する。
8. 新しい問題が一つも見つからない状態になるまで、手順 6 と 7 を繰り返す。
9. 公式リンクと記事内リンクを確認する。
10. `git diff --check` を実行する。
11. 可能なら、起動中の Qiita プレビューまたは `npx qiita preview` で対象記事を表示し、次を確認する。
   - HTTP ステータスが成功である
   - `error_messages` が空である
   - Mermaid、表、コードブロック、注釈が崩れていない
12. 実行できなかった確認があれば、完了報告で明記する。

## 完了条件

次の条件をすべて満たすまで、記事レビューを完了としない。

- 重要な技術的主張を公式情報と照合した。
- 事実と推測を区別した。
- 初出の専門用語に説明がある。
- 前提知識のない読者が、前から順に読める。
- 本文、表、図の各説明で、誰が何をするのかと指示語の参照先が明確である。
- 必須・推奨・任意を混同していない。
- 仕様で定義された要素と説明用の例を区別している。
- 不要な繰り返しと強すぎる断定を取り除いた。
- 同じ種類の問題を記事全体で検索し、最後の通読で新しい問題が見つからなかった。
- コード例と図が本文および公式仕様と矛盾していない。
- 参照リンクが公式ページを直接指している。
- `git diff --check` が成功した。
- 可能な範囲で Qiita のレンダリングを確認した。

## 完了報告

- 最初に、記事へ修正を反映したことを伝える。
- 変更したファイルへのリンクを示す。
- 技術的に重要な修正を箇条書きでまとめる。
- 照合に使った主な公式情報を示す。
- 実施した検証と結果を伝える。
- 検証できなかった項目がある場合だけ、未確認事項として明記する。
- 軽微な言い換えをすべて列挙しない。
